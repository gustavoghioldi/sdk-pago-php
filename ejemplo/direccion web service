direccion web service.
no tomar en cuenta el sessionID.-

pago facil rapi pago.
devuelve el codigo de barra

6. Implementación” habla de enviar el nro de terminal, pero pasa que nunca lo menciono anteriormente, entonces es un error de edición o se olvidaron de poner como implementarlo
